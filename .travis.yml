language: java
jdk:
- openjdk8
before_cache:
- rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
  - "$HOME/.gradle/caches/"
  - "$HOME/.gradle/wrapper/"
install: "./gradlew assemble"
script: "./gradlew clean build bintrayUpload :gql-docs:gitPublishPush"
branches:
  only:
  - master
